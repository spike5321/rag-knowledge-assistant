# 复制本文件为 .env 并填入你的智谱 API Key（申请：https://open.bigmodel.cn）
ZHIPU_API_KEY=your_api_key_here
# 可选，默认 glm-4.5-flash（免费；glm-4-flash 已被智谱下线）
# CHAT_MODEL=glm-4.5-flash
# 可选向量化 provider：auto（默认，优先智谱 embedding-3，无权限时降级本地模型）/ zhipu / local
# 注意：切换 provider 后向量维度不同，需删除 db/ 目录重新入库
# EMBEDDING_PROVIDER=auto
