# 复制本文件为 .env 并填入你的智谱 API Key（申请：https://open.bigmodel.cn）
# 不填也能跑：向量化会自动改用本地模型（首次运行需下载约 100MB）
# ZHIPU_API_KEY=your_api_key_here

# 可选，对话模型，默认 glm-4.5-flash（免费；glm-4-flash 已被智谱下线）
# CHAT_MODEL=glm-4.5-flash

# 可选，向量化 provider：
#   auto（默认）—— 有 Key 就用智谱 embedding-3；没配 Key（或账号无权限）就用本地模型
#   zhipu       —— 只用智谱，失败即报错
#   local       —— 只用本地 fastembed（BAAI/bge-small-zh-v1.5，离线运行）
# EMBEDDING_PROVIDER=auto

# 注意：两个 provider 的向量维度不同（2048 vs 512），切换后必须重建向量库：
#   python -m rag.ingest --rebuild
